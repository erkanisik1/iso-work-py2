#eski ayarlar
#HOOKS="base udev miso miso_loop_mnt miso_kms modconf block filesystems plymouth"
# HOOKS="base udev modconf block miso miso_loop_mnt miso_kms filesystems keyboard fsck"
# MODULES="keyboard nls_ascii nls_cp437 ext4 iso9660 loop squashfs vfat usb_storage usbcore usb-common uhci_hcd ehci_hcd xhci_pci_renesas"
# BINARIES="blkid"

# yamalanmış hali
#HOOKS="base udev modconf plymouth block miso miso_loop_mnt miso_kms filesystems keyboard fsck"
#MODULES="keyboard nls_ascii ext4 iso9660 loop squashfs vfat usb_storage uhci_hcd ehci_hcd dm_mod dm-mod cdrom isofs"
#BINARIES="blkid blockdev losetup switch_root fsck"
#COMPRESSION="xz"


HOOKS="base udev modconf keyboard plymouth block miso miso_loop_mnt filesystems fsck"
#HOOKS="base udev autodetect modconf keyboard block miso miso_loop_mnt miso_overlayfs miso_kms filesystems fsck"

MODULES="nls_ascii uhci_hcd ehci_hcd usb_storage loop squashfs iso9660 isofs ext4 vfat cdrom dm_mod overlay"

BINARIES="blkid blockdev losetup switch_root fsck"

COMPRESSION="xz"

